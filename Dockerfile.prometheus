# Use official Prometheus image  
FROM prom/prometheus:v2.51.2

# Copy simple static config (will be updated manually via ALB DNS)
COPY infra/monitoring/prometheus-simple.yml /etc/prometheus/prometheus.yml

USER root

# Use default Prometheus entrypoint, it will use our config
