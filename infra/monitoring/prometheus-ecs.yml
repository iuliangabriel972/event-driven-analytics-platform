global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "telemetry-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["10.0.1.250:8000"]
        labels:
          service: "telemetry-api"
          environment: "production"

  - job_name: "analytics-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["10.0.2.17:8001"]
        labels:
          service: "analytics-api"
          environment: "production"

  - job_name: "processor"
    metrics_path: "/metrics"
    ec2_sd_configs:
      - port: 8002
        filters:
          - name: tag:Service
            values: ["event-platform-processor"]
